# Mkdocs build on readthedocs.org cannot find pyalgotrading package while building, hence this custom requirements file which explicitly adds the package
# Make sure this is kept in sync with requirements.txt for all packages but pyalgotrading
pyalgotrading

pandas>=1.0.3
requests>=2.23.0
mkdocs>=1.1
mkdocs-material>=5.1.5
mkdocs-git-revision-date-localized-plugin>=0.5.2
mkdocstrings>=0.11.2
twine>=3.1.1

